import { describe, expect, it } from 'vitest'; import { ObjectiveTracker } from '../../src/combat/Objectives'; import { battleDefinition, units } from '../fixtures/combatFixture';
describe('ObjectiveTracker', () => { it('avoids premature completion, detects victory, and detects loss', () => { const tracker = new ObjectiveTracker(battleDefinition().objectives); const values = units(); expect(tracker.evaluate(values)).toBe('ongoing'); values[1].alive = false; expect(tracker.evaluate(values)).toBe('won'); values[0].alive = false; expect(tracker.evaluate(values)).toBe('lost'); }); });
