export class SeededRandom { constructor(private stateValue: number) { this.stateValue >>>= 0; if (this.stateValue === 0) this.stateValue = 0x6d2b79f5; } get state(): number { return this.stateValue >>> 0; } set state(value: number) { if (!Number.isInteger(value)) throw new Error('PRNG state must be an integer.'); this.stateValue = value >>> 0; } next(): number { let value = this.stateValue += 0x6d2b79f5; value = Math.imul(value ^ value >>> 15, value | 1); value ^= value + Math.imul(value ^ value >>> 7, value | 61); this.stateValue = value ^ value >>> 14; return (this.stateValue >>> 0) / 4294967296; } roll(probability: number): boolean { return this.next() < Math.min(1, Math.max(0, probability)); } }
