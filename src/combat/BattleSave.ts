import type { BattleObjectiveState, UnitBattleState, UnitId } from './CombatTypes'; import type { InitiativeState } from './Initiative'; import type { BattlePhase } from './TurnStateMachine'; import { cloneUnit } from './UnitModel';
export const BATTLE_SAVE_VERSION = 1;
const phases = new Set(['battleInitializing','initiativeAdvancing','turnStarting','awaitingCommand','previewingMovement','executingMovement','choosingAction','choosingTarget','previewingAction','executingAction','resolvingEffects','turnEnding','enemyThinking','battleWon','battleLost','battlePaused','fatalError']);
export interface BattleSnapshot { readonly version: number; readonly mapId: string; readonly units: readonly UnitBattleState[]; readonly activeUnitId?: UnitId; readonly initiative: InitiativeState; readonly phase: BattlePhase; readonly hasMoved: boolean; readonly hasActed: boolean; readonly objectives: readonly BattleObjectiveState[]; readonly randomState: number; readonly log: readonly string[]; }
export function createBattleSnapshot(mapId: string, units: readonly UnitBattleState[], activeUnitId: UnitId | undefined, initiative: InitiativeState, phase: BattlePhase, hasMoved: boolean, hasActed: boolean, objectives: readonly BattleObjectiveState[], randomState: number, log: readonly string[]): BattleSnapshot { return { version: BATTLE_SAVE_VERSION, mapId, units: units.map(cloneUnit), activeUnitId, initiative, phase, hasMoved, hasActed, objectives: objectives.map((o) => ({ ...o })), randomState, log: log.slice(-50) }; }
export function validateBattleSnapshot(input: unknown): input is BattleSnapshot { if (!record(input) || input.version !== BATTLE_SAVE_VERSION || typeof input.mapId !== 'string' || !Array.isArray(input.units) || !input.units.every(validUnit) || !record(input.initiative) || typeof input.initiative.paused !== 'boolean' || (input.initiative.activeUnitId !== undefined && typeof input.initiative.activeUnitId !== 'string') || !phases.has(input.phase as string) || typeof input.hasMoved !== 'boolean' || typeof input.hasActed !== 'boolean' || !Array.isArray(input.objectives) || !Number.isInteger(input.randomState) || !Array.isArray(input.log) || !input.log.every((v) => typeof v === 'string')) return false; return true; }
function validUnit(value: unknown): boolean { return record(value) && typeof value.id === 'string' && typeof value.templateId === 'string' && typeof value.displayName === 'string' && record(value.position) && Number.isInteger(value.position.x) && Number.isInteger(value.position.y) && finite(value.currentHp) && finite(value.maxHp) && finite(value.chargeTime) && typeof value.alive === 'boolean' && Array.isArray(value.statuses); }
function record(value: unknown): value is Record<string, unknown> { return typeof value === 'object' && value !== null && !Array.isArray(value); } function finite(value: unknown): value is number { return typeof value === 'number' && Number.isFinite(value); }
