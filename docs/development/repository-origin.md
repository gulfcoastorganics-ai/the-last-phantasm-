# Repository Origin

The configured Git remote was verified on 2026-07-21:

```text
https://github.com/gulfcoastorganics-ai/the-last-phantasm-.git
```

The requested `/mnt/data/git-repo.txt` source was unavailable, so this record is based on direct Git configuration inspection rather than a file import.
