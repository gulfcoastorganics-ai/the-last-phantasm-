import type { Scene } from '../core/Scene'; import type { TimeSnapshot } from '../core/Time'; import type { CanvasRenderer } from '../rendering/CanvasRenderer'; import type { AppShell } from '../ui/AppShell';
export class MainMenuScene implements Scene { readonly id = 'menu'; constructor(private readonly renderer: CanvasRenderer, private readonly shell: AppShell) {} enter(): void { this.shell.showMenu(true); } update(_time: TimeSnapshot): void {} render(): void { this.renderer.begin('#100d17'); const { context: c } = this.renderer; const { width, height } = this.renderer.viewport.size; const glow = c.createRadialGradient(width * .7, height * .25, 0, width * .7, height * .25, Math.max(width, height) * .7); glow.addColorStop(0, '#402745'); glow.addColorStop(.5, '#171222'); glow.addColorStop(1, '#09070d'); c.fillStyle = glow; c.fillRect(0, 0, width, height); } exit(): void { this.shell.showMenu(false); } dispose(): void {} }
