import type { RenderPass, TacticalRenderState } from './RenderPass';
export class UiPass implements RenderPass { readonly id = 'ui'; render(state: TacticalRenderState): number { if (!state.debug || !state.hovered) return 0; const tile = state.map.get(state.hovered.x, state.hovered.y); if (!tile) return 0; const context = state.renderer.context; context.fillStyle = 'rgba(5,7,10,.8)'; context.fillRect(12, 12, 220, 48); context.fillStyle = '#f5e9d2'; context.font = '13px ui-monospace, monospace'; context.fillText(`${tile.x},${tile.y} · ${tile.terrain.name}`, 22, 32); context.fillText(`elevation ${tile.elevation} · cost ${tile.movementCost}`, 22, 50); return 2; } }
