import type { Camera, Point } from '../Camera'; import type { CanvasRenderer } from '../CanvasRenderer'; import type { TacticalMap } from '../../data/tactical/TacticalMap'; import type { IsometricGrid } from '../../world/IsometricGrid'; import type { TileHighlights } from '../TileHighlights'; import type { GridPoint } from '../../world/GridCoordinates'; import type { UnitBattleState, UnitId } from '../../combat/CombatTypes';
export interface TacticalRenderState { readonly renderer: CanvasRenderer; readonly camera: Camera; readonly map: TacticalMap; readonly grid: IsometricGrid; readonly highlights: TileHighlights; readonly selected?: GridPoint; readonly hovered?: GridPoint; readonly debug: boolean; readonly path: readonly GridPoint[]; readonly units?: readonly UnitBattleState[]; readonly activeUnitId?: UnitId; readonly selectedUnitId?: UnitId; readonly hoveredUnitId?: UnitId; readonly impactUnitId?: UnitId; readonly targetableUnitIds?: ReadonlySet<UnitId>; readonly presentationPositions?: ReadonlyMap<UnitId, Point>; }
export interface RenderPass { readonly id: string; render(state: TacticalRenderState): number; }
