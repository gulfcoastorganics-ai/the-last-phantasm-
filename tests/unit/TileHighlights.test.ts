import { describe, expect, it } from 'vitest'; import { TileHighlights } from '../../src/rendering/TileHighlights';
describe('TileHighlights', () => { it('layers highlights deterministically by semantic priority', () => { const highlights = new TileHighlights(); highlights.set('range', [{ point: { x: 1, y: 2 }, kind: 'movement' }]); highlights.set('selection', [{ point: { x: 1, y: 2 }, kind: 'selection' }, { point: { x: 0, y: 0 }, kind: 'objective' }]); expect(highlights.ordered()).toEqual([{ point: { x: 0, y: 0 }, kind: 'objective' }, { point: { x: 1, y: 2 }, kind: 'selection' }]); }); });
